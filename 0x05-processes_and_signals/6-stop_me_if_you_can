#!/usr/bin/env bash
# Ts script terminates the 4-to_infinity_and_beyond process without using kill or killall.

# Get the PID of the running 4-to_infinity_and_beyond process
PID=$(pgrep -f "4-to_infinity_and_beyond")

# Send the SIGTERM signal to terminate the process
echo 15 > "/proc/$PID/sigact
