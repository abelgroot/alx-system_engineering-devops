#!/usr/bin/env bash
# This script stops the 4-to_infinity_and_beyond process.

# Find the PID of the process using pgrep and kill it
PID=$(pgrep -f "4-to_infinity_and_beyond")

if [ -n "$PID" ]; then
    kill "$PID"
    echo "Terminated"
else
    echo "No process found"
fi
